spending gift //script_purpose script_name

 

// validator function: no input and returns a Bool: true
func main(_, _, _) -> Bool {
    true
}

// Validation will always succeed for any transaction grabbing a utxo held at this script's address.